import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.app',
  appName: 'refer-mate-v1',
  webDir: 'dist/refer-mate-v1/browser'
};

export default config;
